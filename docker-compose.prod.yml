services:
  app:
    image: ${DOCKER_IMAGE}
    container_name: wander-jr-prod
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env.prod
    volumes:
      - ./data/documents:/app/data/documents
